# Offline visual preview

Open `index.html` directly in a browser to review the product direction without installing Node packages. This page is a non-production visual replica; the maintained implementation is the React app in `apps/web`.

Captured desktop, results, and mobile views are stored in `../artifacts/screenshots/`.
