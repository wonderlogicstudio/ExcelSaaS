# WorkbookCare 시작 안내서

이 저장소는 단순한 홈페이지 시안이 아니라 다음 두 가지를 함께 제공합니다.

1. **실제로 실행 가능한 React + FastAPI 프로토타입**
2. **Codex가 임의로 제품 방향을 확장하지 못하도록 통제하는 제품 개발 운영체계**

작업명은 `WorkbookCare`입니다. 도메인이나 상표를 구입하기 전에 반드시 최종 브랜드명을 다시 정해야 합니다.

---

## 1. 현재 만들어진 범위

현재 버전에서 가능한 것:

- 한국어 반응형 랜딩페이지
- 가입 없는 샘플 진단 체험
- `.xlsx`, `.xlsm` 파일 선택 및 업로드
- Python/FastAPI 기반 정적 워크북 검사
- 깨진 참조, 외부 통합문서 참조, 휘발성 함수, 깊은 IF, 전체 열 참조, 숨김 시트, VeryHidden, 매크로·차트 존재 등을 탐지
- 숫자 열 안에서 숫자가 텍스트로 저장된 고신뢰 후보 탐지
- 위험도, 복잡도, 자동 수정 후보, 확인 필요, 전문가 검토 분류
- 결제 전에 포함·제외 범위와 파일별 예상 가격 표시
- OOXML ZIP 안전성 검사
- 원본 파일 미변경
- VBA·수식·외부연결 미실행
- 백엔드 자동 테스트와 오프라인 시각 미리보기

아직 의도적으로 만들지 않은 것:

- 실제 결제
- 실제 파일 자동수정
- 회원가입
- 사용자 파일의 클라우드 보관
- Cloudflare R2 및 Cloud Run 실배포
- 전문가 상담 시스템
- 실사용 약관·개인정보처리방침

결제와 자동수정을 먼저 붙이지 않은 이유는, 사용자가 **진단 결과를 이해하고 신뢰하며 제시 가격을 받아들이는지**부터 검증해야 하기 때문입니다.

---

## 2. 가장 빠르게 화면 확인하기

Node나 Python을 설치하지 않고 디자인만 확인하려면:

```text
preview/index.html
```

파일을 Chrome이나 Edge에서 직접 여십시오.

이 페이지는 제품 방향 확인용 오프라인 복제본입니다. 실제 유지보수 대상은 `apps/web`의 React 코드입니다.

시각 검증 결과는 다음 폴더에 있습니다.

```text
artifacts/screenshots/
```

---

## 3. Windows에서 실제 서비스 실행하기

### 사전 준비

- Node.js 22 이상
- Python 3.12 이상
- VS Code
- Codex IDE 확장

### 최초 1회 설치

프로젝트 루트에서 PowerShell을 열고:

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\scripts\bootstrap.ps1
```

직접 설치하려면:

```powershell
npm install

cd apps\api
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e ".[dev]"
cd ..\..
```

### 프론트엔드와 백엔드 동시에 실행

```powershell
.\scripts\dev.ps1
```

접속 주소:

```text
웹사이트: http://localhost:5173
API 문서: http://localhost:8000/docs
```

### 테스트용 Excel 생성

```powershell
cd apps\api
.\.venv\Scripts\Activate.ps1
python ..\..\scripts\generate_sample_workbook.py
```

생성 위치:

```text
samples\demo-risky-workbook.xlsx
```

이 파일은 전부 합성 데이터이며 의도적인 오류가 포함되어 있습니다.

### 전체 검증

프로젝트 루트에서:

```powershell
.\scripts\verify.ps1
```

---

## 4. Codex를 사용하는 방식

### 핵심 역할 분리

사람이 결정하는 것:

- 누구의 어떤 문제를 해결할지
- 어떤 결과가 유료 가치가 있는지
- 가격과 지원 범위
- 개인정보·보안 기준
- 다음 마일스톤 승인 여부

Codex가 결정하는 것:

- 승인된 범위 안의 코드 구조
- 컴포넌트와 함수 설계
- 라이브러리 사용 방법
- 테스트 구현
- 오류 수정과 리팩터링 방법

즉, 사용자가 매번 “이 파일을 만들고 이 함수명을 써라”라고 명령하지 않습니다.

### 최초 Codex 실행

VS Code에서 프로젝트 **루트 폴더 전체**를 연 뒤 Codex Goal에 다음 파일 내용을 붙여넣습니다.

```text
prompts/01_FIRST_CODEX_GOAL.md
```

이 프롬프트는 다음만 수행합니다.

1. 기존 M1/M2 기준선 확인
2. 테스트 및 실행 상태 확인
3. 사용자 테스트를 막는 실제 결함 보고
4. M3 범위 추천
5. 멈춤

현재 M3는 아직 승인되지 않았기 때문에 Codex가 자동으로 다음 기능을 개발하면 안 됩니다.

### 먼저 Git 기준점 만들기

최초 설치와 검증이 끝나면 프로젝트 루트에서:

```powershell
git init
git add .
git commit -m "WorkbookCare M0-M2 baseline"
```

이 기준점이 있어야 Codex가 변경 내용을 정확히 비교하고 문제가 생기면 쉽게 되돌릴 수 있습니다. 실제 고객 파일이나 `.env` 비밀값은 커밋하지 마십시오.

### 다음 마일스톤 승인

화면을 직접 검토한 후 M3를 진행하기로 결정하면 다음 파일 내용을 보냅니다.

```text
prompts/02_APPROVE_AND_CONTINUE.md
```

Codex는 M3 하나만 구현하고 다음 파일을 만든 뒤 정지해야 합니다.

```text
docs/MILESTONE_REVIEW.md
```

### 반복 주기

```text
사람이 현재 결과 확인
→ 제품 방향 또는 마일스톤 승인
→ Codex가 구현·테스트·문서화
→ Codex 정지
→ 사람이 승인/수정/거절
```

이 방식으로 긴 명령어를 매번 다시 만들 필요가 없습니다.

---

## 5. 토큰을 적게 쓰는 구조

무료 파일 진단에는 원칙적으로 LLM을 사용하지 않습니다.

```text
Excel 파일
→ Python 규칙 검사
→ 구조화된 JSON
→ 고정 설명 템플릿
→ 위험도·견적 규칙
```

Codex 개발 토큰은 다음 방식으로 통제합니다.

- 한 번에 한 마일스톤
- `docs/00_CONTEXT_INDEX.md`에서 필요한 문서만 선택
- 전체 저장소 재분석 금지
- 병렬 에이전트 기본 금지
- 이미 통과한 기능 재작성 금지
- 자동 테스트로 검증
- 완료 기준 통과 즉시 정지
- 다음 마일스톤은 사람만 승인

자세한 기준:

```text
docs/15_AI_TOKEN_BUDGET.md
docs/17_CODEX_OPERATING_GUIDE.md
```

---

## 6. 제품 방향

범용 “AI Excel 챗봇”으로 경쟁하지 않습니다. Microsoft와 Google의 내장 AI가 수식 생성, 분석, 표·차트 작성까지 빠르게 흡수하고 있기 때문입니다.

우리의 초기 카테고리:

> **Workbook diagnosis and approved repair**  
> 엑셀을 고치기 전에, 무엇을 왜 고칠지 먼저 보여주는 서비스

초기 핵심 사용 순간:

> 중요한 Excel 파일을 고객, 상사, 회계사, 기관에 보내기 직전

핵심 흐름:

```text
무료 검사
→ 정확한 위치와 근거
→ 안전 수정 / 확인 필요 / 전문가 검토 분류
→ 포함·제외 범위와 가격
→ 사용자 승인
→ 별도 수정본
→ 변경 내역과 재검사
```

---

## 7. 지금 가장 먼저 테스트할 것

아직 자동수정 정확도를 확장하지 마십시오. 먼저 10명에게 다음을 확인합니다.

1. 첫 화면을 5초 본 뒤 서비스가 무엇인지 설명할 수 있는가?
2. 무료 진단과 유료 수정의 차이를 이해하는가?
3. 셀 위치와 규칙 코드가 신뢰를 높이는가?
4. 포함·제외 범위를 스스로 찾을 수 있는가?
5. 실제 문제 파일이라면 제시 금액을 낼 의향이 있는가?
6. 회사 파일 업로드를 막는 가장 큰 이유는 무엇인가?

테스트 방법과 합격 기준:

```text
docs/18_USER_TEST_PLAYBOOK.md
```

M3는 이 테스트를 체계적으로 기록하기 위한 최소 계측과 피드백 UI입니다.

---

## 8. 호스팅 준비 상태

목표 구조:

```text
Cloudflare Workers Static Assets
        ↓
React 웹
        ↓
R2 단기 서명 업로드
        ↓
Cloud Run Python 분석 컨테이너
        ↓
결과 JSON / 별도 수정본
```

현재는 계정·비용·개인정보 처리를 발생시키지 않도록 로컬 구조만 준비했습니다.

실배포 전 필수:

- 도메인 결정
- Cloudflare 계정
- Google Cloud 프로젝트와 결제 한도
- R2 수명주기 삭제 정책
- Rate limit
- 악성 파일 차단
- 비밀키 관리
- 개인정보처리방침과 이용약관
- 장애 및 환불 절차

관련 문서:

```text
docs/14_HOSTING_DEPLOYMENT.md
infra/
```

---

## 9. 중요한 제한

- 현재 위험 점수는 손실 확률이 아니라 발견된 구조적 위험의 우선순위 점수입니다.
- 정적 분석은 Excel 계산 엔진을 실행하지 않습니다.
- 업무 의미가 틀린 정상 형식의 수식은 놓칠 수 있습니다.
- `openpyxl`로 임의의 고객 파일을 열고 다시 저장하면 일부 도형이나 비지원 요소가 손상될 수 있으므로, 현재 버전은 분석 후 저장하지 않습니다.
- `.xlsm`은 매크로를 실행하지 않고 전문가 검토로 분류합니다.
- 실제 자동수정은 변경 미리보기, 별도 파일, 재검사, 롤백 가능성 검증 후에만 열어야 합니다.

---

## 10. 제품 소유자가 지금 할 일

1. `preview/index.html`을 열어 첫 화면과 샘플 결과를 확인합니다.
2. 문구·가격·신뢰 요소에서 수정할 부분을 기록합니다.
3. 로컬 앱을 실행해 합성 Excel을 업로드합니다.
4. M3 사용자 테스트 계측을 승인할지 결정합니다.
5. 승인 전에는 결제·회원가입·클라우드 저장을 개발하지 않습니다.
