# APPROVED — Proposal-led repair and optional user intent

Latest owner request 2026-09-13: users need supported proposals and understandable examples, not cell-address/field-role knowledge. This bounded follow-up reuses the existing free scanner, private RP01/RP02 engine and entitlement/approval/delivery boundaries.

1. Step2 offers sheet selection and rule-based groups from existing diagnostic candidates, grouped by sheet/repair kind/column. Show eligible-to-check, absent and unsupported states accurately; a candidate is never an eligibility guarantee. Existing selected subsets remain selectable, no hidden expansion. Manual coordinates remain a secondary path.
2. Replace field-role jargon with concrete amount/count/identifier questions. Explicit user business confirmation remains required. Offer real source-cell previews and existing nearby formula anchors as selectable suggestions; never auto-confirm their business correctness or invent a formula.
3. Optional structured intent from free diagnosis: expected numeric result or a chosen source cell's calculation rule. Default has no opinion. Keep same static detection and disclose how the request will be checked after calculating a supported plan. Intent stays in this browser session, no LLM/telemetry/private feedback extension. Unsupported wishes (existing SUM range/outlier rewrite, arbitrary values or B-as-truth) remain unsupported, not silently applied.
4. Supported plans show one representative actual before/after result first, followed by the affected scope and expandable complete exact changes. Values require the existing validated engine and existing rights; do not fabricate pre-entitlement computed values. Source cache, structural proposal and engine-calculated result have distinct labels. Preserve all exact detail and separate approval before execution.
5. Match user-requested values against the complete selected plan's actual calculated result without changing values to force a match. Missing/mismatched/unsupported requests block the proposal's approval with an explanation. Source, selection, criteria and intent changes invalidate displayed proposal agreement. Do not treat notes, representative preview or payment as exact change approval.
6. Reuse bounded browser ZIP evidence reader for metadata and explicitly requested cell previews; preserve size/CRC/path/XML/cancellation safeguards and fail closed. Do not loosen API/engine/Access/private storage/commerce gates. No runtime dependencies or remote AI required.
7. Baseline121/type/build. Test rule cases, zero/unsupported sheets, selected subsets, identifier/blank distinctions, anchor choice, opinion absent/match/mismatch/unverifiable, stale request and approval separation. Full existing verify + M4. Inspect actual Access-beta desktop/mobile, fixed expected values and actual downloads/Excel for supported cases. Freeze existing samples/oracles. Record failures, commands/exits, versioned proof and NOT_RUN. Selective local commit, no push, stop for owner review. D08 overall remains PARTIAL.
