## Resume01 - functional review PASS; UX target pending (2026-09-23)

User explicitly approved correcting/retesting the stopped test. Contradictory expectation replaced with a real generic-nonmonthly negative; value-free monthly candidate still enters server validation. Restored actual-source before_formula assertions in three existing RP03 regression cases. Corrected approval/provenance wording only; no engine/oracle changes.

Fresh: App.navigation14 PASS; targeted monthly3 PASS/19 skipped; final TypeScript/Vite build exit0. Piped shell filter initially exited255 before Vitest; safe filter passed. Logs retained under stage4/resume01. Remaining unchanged successful tests reused, not counted as new runs. Independent source and regression review PASS.

Actual Chrome localhost5186: fixed6-sheet source -> structure0/formula1 -> original formula automatically read -> server preflight/plan -> visible #VALUE! to-5 -> exact N18 before/after formula -> keyboard approval checkbox -> separate approval -> READY -> three actual downloads. Downloaded bytes match the private stored artifacts; raw formula and cached-5 match the native-validated stage3 repaired file. Original hash preserved. Desktop1440 and mobile390 screenshots saved. Reviewer independently inspected outputs and screenshots: bounded functional PASS.

Existing local synthetic operator grant was used; payment OFF and not PG/commercial evidence. Browser download-event wait timed out but actual file existed and matched bytes; no repeated download click. Actual source/formula typing0, four tabs. Observed14 core app actions vs initial10 target (includes exact-detail expansion; excludes OS chooser/operator refresh/inspection clicks). UX target NOT MET; no human-usability PASS. This does not authorize broad UX redesign.

Current native reference fingerprints and frozen oracle/user-lock hashes remain valid. No native rerun, full repository regression, Git commit/push or beta deployment. Prior failures and preservation incident remain below. Next one proposed scope: reduce the required actions without weakening approval/evidence. Authoritative resumed result: artifacts/synthetic_validation/monthly-repair-flow06/stage4/resume01/result.json; ui-result.json; browser-downloads/; desktop-example.png, mobile-example.png, desktop-ready.png, mobile-ready.png.

---

# MONTHLY-REPAIR-FLOW-06 - stages 1-3 PASS; stage4 STOPPED (2026-09-22)

Scope: user approved stages1-4 with internal review between stages. No sheet-limit expansion, Git or deployment. Existing RP01/RP02/combined repair and exact customer approval remain required.

## Completed and reused

Stages1/2 separate strict monthly rule and profile-specific input/before-error policy passed independent review. Stage3 genuine patch/calculation, source preservation, exact approval and three-file delivery passed. Frozen supported6-sheet Budget!N18 changes from =N15-N14 (#VALUE!) to ='M10'!B16-'M10'!B15, result -5 from1001-1006; candidate1->0. Original13-sheet oracle is unchanged and still exceeds10-sheet limit.

Actual installed Excel16.0 validated repaired+changes XLSX for RP01/RP02/RP03 (six XLSX), native expected values and unchanged file hashes. Final compatibility evidence was reviewed then registered. Unmocked RP03 approval->READY->three saved downloads passed; repaired raw formula and cached-5 match. Existing calculation20/native20 prior-unit evidence was reused, not rerun as a full loop.

Stage3 commands/exits: artifacts/synthetic_validation/monthly-repair-flow06/stage3/final-verification.json. Saved files: stage3/saved-artifacts03. Initial native command failed because PowerShell5.1 interpreted UTF8 without BOM incorrectly; encoding-only fix, retry exit0. Failed evidence retained.

## Stage4 incomplete

Frontend connects a validation candidate using the original formula read by existing readSourceCells, retains server eligibility, zero mandatory address/formula input and separate approval. Monthly example uses actual impact.before/after. Mixed monthly/legacy selections are rejected without silently removing existing selections. Initial reviewer found fabricated API evidence fields and incorrect example/test shape; these were corrected without expanding the value-free API contract or changing backend/native fingerprints.

Initial targeted41 tests and build passed on pre-review code; those are not final corrected-source proof. Correction tests ran twice and exited1. Latest:40passed/1failed, ProposalFlow22 and ProposalExample5 pass. App.navigation.test.tsx86-87 contains contradictory adjacent undefined/RP03 expectations for the same monthly finding. No further correction or rerun after the retry limit. Final build, final review and full regression remain NOT_RUN.

Actual Chrome localhost5186 combined diagnostic screen shows structure0/formula candidate1 for the frozen source. Remaining: monthly proposal/preflight/example-5, exact approval/downloads through UI, desktop/mobile visual checks. Initial local internal-beta presentation separated audit; restarted only the local frontend with hosted-beta integrated presentation, still pointing to isolated internal-beta localhost API, paymentOFF. Not a beta deployment. No human usability or click-budget pass claimed. Browser upload tool latency is not product processing latency.

## Preservation incident

An initial compatibility bootstrap wrote to historical d08 compatibility. Nine prior files were recovered from a verified archive whose native record exactly matched the untouched old record; independent review confirmed all restored hashes. New artifacts were isolated, nonempty output refusal and manifest/native hash binding added. Unknown prior private bootstrap database state is not claimed restored. Full recovery details: stage3/preservation-incident.json and stage3/recovery.json. This incident is retained, not erased by later PASS.

## Stop / next

EFFICIENCY-02 retry limit applies to the unresolved stage4 test failure; STOP pending owner decision. Next one unit: correct the stale contradictory assertion, run affected checks and finish actual local UI verification. Do not repeat unchanged native compatibility tests. No commit/push/deploy, new prices/resources/live transactions or actual customer files occurred.

Authoritative final status, local server sessions, immutable hashes and pending checks: artifacts/synthetic_validation/monthly-repair-flow06/final-status.json. Prior progress records preserved.
