# Guided Flow — 단계별 탭과 이해할 수 있는 무료 진단

2026-09-13 · **ENGINEERING_VERIFIED_AWAITING_OWNER / 작업 정지**. 최신 8개 피드백을 반영한 한정 UX 후속입니다. D08 전체는 **PARTIAL**이며 다음 묶음은 실행하지 않았습니다. [보호 베타](https://workbookcare-beta.wonderlogic-studio.workers.dev/), [기계 판독 증거](evidence/GUIDED_FLOW.json).

## 사용자 의견에 대한 판단과 실제 보완

진단 결과를 이해하기 전에 검토 선택·확인함·견적이 함께 나와 다음 행동이 불명확했던 지적이 맞습니다. 다만 주변 패턴만으로 정상 수식이나 업무 정답을 확정할 수는 없습니다. 무료 단계는 실제 원본 비교로 이유를 설명하고, 지원하는 변경의 계산 결과를 정확한 승인 단계에서 보여줍니다.

| 피드백 | 실제 구현 |
|---|---|
| 단계가 섞여 보임 | **무료 진단 → 수정 범위·검증 → 변경 승인 → 결과 받기** 4개 탭. 선행 조건 완료 후 해제하고 이전 단계로 돌아가도 선택·메모·작업 유지 |
| 확인 전/확인함/선택 불명확 | ‘메모 없음’, ‘내용을 읽어봄’ 등 선택적 개인 메모로 변경. ‘표시된 N개를 2단계에 포함’과 분리; 읽어봄만으로 선택·승인하지 않음 |
| 실제 수식·차이가 안 보임 | 펼친 셀에 원본 수식/문자/상수/빈 셀, 기존 탐지기가 제공한 비교 위치 최대2개, 탐지한 차이를 나란히 표시 |
| 예상 결과가 필요함 | 저장된 값은 ‘재계산하지 않은 값’. 검증된 변경 후 수식·계산값·영향은3단계의 실제 기존 서버 계획에서 표시. 미지원 수식의 정답이나 수정안을 생성하지 않음 |
| 설명·한계 반복 | 검사 범위·한계는 결과 상단 한 곳. 고유 규칙 설명은 보존; 유형 공통 설명은 한 번, 셀에는 해당 차이 |
| 무료 단계의 견적 혼란 | 무료 결과의 참고 견적 노출 제거. 기존 견적 코드·데이터는 보존. 지원 범위 확인 뒤에 현재 가격 미확정·합성 시험 상태를 정확히 안내 |
| 정밀 검증의 비용·필요성 불명확 | 현재 무료 패턴 근거, 지원 수정의 계산 검증, 미구현 추가 업무 검증을 구별. 유료 정밀 검증을 필수 선행 구매나 판매 가능한 기능으로 표시하지 않음 |
| 사전 확인/수정 범위 중복 | 결과 목록 아래 선택 수와 다음 단계 CTA로 통합.2단계는 선택 범위·지원 여부·업무 기준을 다루며 발견 근거를 중복하지 않음. 수동 셀 지정은 보조 진입으로 유지 |

## 재사용과 안전 경계

L01–L08·CF·UXR의 필터/전체 표/위치·근거·구조 CSV/위험 점수/직접 재검사와 M4 값 없는 API를 재사용했습니다. 기존 D02–D08 원본 고정·RP01/RP02·보존 패치·격리 계산·정확한 계획·권리·별도 승인·실행·세 파일 납품, H3 private/default-off도 유지합니다. 두 파일 비교는 별도 보고서 상품이며 B를 정답으로 삼지 않습니다.

브라우저는 사용자가 올린 같은 File에서 해당 위치만 필요할 때 읽습니다. 입력10MiB, ZIP2048항목/선언합64MiB, XML8MiB/메타1MiB, 셀10만/표시문자2048 제한과 CRC·경로·중복·암호화·크기·취소/시간 제한을 적용했습니다. 외부 연결·수식 실행·새 API 전송·영속 저장·셀 로그는 없습니다. 공유/배열 수식이나 지원하지 않는 브라우저/파일은 표시 불가이며 빈 셀로 오인하지 않습니다. 비교 위치는 탐지기가 제공한 첫4개 중 가까운2개이므로 바로 위아래 행이라는 보장은 없습니다. Node24 타입만 개발 의존성으로 추가했고 런타임 패키지는 추가하지 않았습니다.

계획이 바뀌거나 만료·취소되면 뒤 단계와 오래된 승인 상세를 닫습니다. 새 파일은 상태를 초기화합니다. 원본 동의·업무 역할·기준 수식·변경 승인은 자동 입력하지 않습니다. 권리/결제, 선택, 읽어봄 메모는 변경 승인이 아닙니다.

## 실제 화면과 예상 값

기존 고정 `WorkbookCare_Complex_Validation_2026-09-13`의 expected와 원본을 변경하지 않았습니다. 패키지 예시나 모형 시험을 실제 제품·Excel·PG 성공으로 세지 않았습니다.

| 실제 검증 | 결과 |
|---|---|
| 복합01 무료 | 구조1 + 수식15 = **16건/3유형**, 공통 한계1곳, 무료 견적0. 최종 배포에서도 재확인 |
| 원본 F22 | `=C22*D21` 대 비교F11 `=C11*D11`/F10 `=C10*D10`. 저장값2384/4810/3756은 재계산 아님 |
| 상수/빈 셀 | I95 **42**, I145 **빈 셀**과 각각 I94/I96, I144/I146 실제 수식 비교 |
| 정상 예외02 | 구조0/수식0, 검사 완료와 업무 정확성 보장의 차이 표시 |
| RP01 일부 | B12/B24/B78/B111 **4셀**, 비선택4셀 보존. 정확한 변경4개·간접 영향20개 전후 값 UI 대조. J130 **2,029,526** |
| RP02 빈 셀 | F31/F64/F107의 실제 복원 수식과 **5,232 / 3,551 / 7,800**, 계산 위치16개 대조. J130 **937,923** |
| 메모/승인/납품 | 읽어봄8개만으로 선택0 유지 →8개 선택→4개로 축소, 탭 왕복 보존. 권리 뒤에도 별도 승인 요구. 승인 직후 자동 실행/다운로드 없음 → 별도 실행 → 각각3파일 |
| 지원 경계04 | 수정 후보0, 구조 위험1. 직접 지정 거래원장C2는 형식 조건0건, 미지원 함수/차트/표 등 이유, 승인·수령 탭 잠금. 계산·구매·수정 진행 없음 |

실제 베타 다운로드 **6개**의 수정본마다725셀 예상값, 변경내역4/3행, HTML9검사와 영향20/13개, 원본·비대상·7 ZIP member 보존을 대조했습니다. 설치된 **Excel16.0/build20326**으로 실제 XLSX4개를 열어 수정본 각725셀 재계산·수식602/605개·비대상 원본값854/858개를 확인했습니다. 읽기 전용으로 종료하고 다운로드 hash 불변입니다. Excel 출력 PDF2페이지도 이미지로 직접 확인했습니다. HTML 자체를 브라우저로 여는 시험은 이번에 재실행하지 않았습니다.

1440/1024/390/412폭에서 화면을 봤습니다. 초기 모바일의 단계 전환 시 탭이 가려지는 현상을 수정했고 최종412폭에서4개 탭과 CTA를 다시 확인했습니다. [최종 모바일 탭](../../../artifacts/screenshots/guided-flow/09-final-step2-mobile.jpg), [실제 원본 수식](../../../artifacts/screenshots/guided-flow/03-original-F22.jpg), [최종 빈 셀 비교](../../../artifacts/screenshots/guided-flow/13-RP02-original-blank.jpg), [정확한 계산값](../../../artifacts/screenshots/guided-flow/14-RP02-calculated-values.jpg), [지원 제외](../../../artifacts/screenshots/guided-flow/19-unsupported-blocked.jpg).

증거01–07은 첫 Worker `d466d4e3-eef5-4b0d-960b-f072e04224d4`,08이후는 최종 `4f756e00-dc3d-4c2c-ae5c-862e79b6422c`입니다. 두 버전 차이는 모바일 스크롤 위치/포커스 테두리입니다.21번 캡처는 접힌 상태라 원본 근거 검증 성공으로 세지 않고22번 펼친 화면으로 보완했습니다. 이전 단계의 전체 위치·비교888행 검증은 기존 증거이며 이번에 전부 다시 했다고 주장하지 않습니다.

## 명령·실패 수정·재시험

프로젝트 루트에서 실행했습니다. 전체 상세 exit code, 각 배포 단계 명령, 로그 hash와 실행 helper는 [증거 JSON](evidence/GUIDED_FLOW.json) 및 `artifacts/verification/guided-flow/`에 있습니다.

| 명령 | 결과 |
|---|---|
| `npm.cmd run test --workspace @workbookcare/web` 시작 기준 | 95 PASS, exit0 |
| `npm.cmd run verify --workspace @workbookcare/web` 표적 보완을 포함한 최종 | **121 PASS/25파일**, TypeScript·production build PASS, exit0 |
| `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-m4-release.ps1 -AcceptProductOwnerFixtureWaiver` | 내부 전체 `verify.ps1` 포함, 웹118/Worker16/API231/Ruff/M4 exact36 PASS, exit0. 기존 CONDITIONAL_GO fixture waiver 유지 |
| `<scratch>/verify_plan_ui.py RP01_SUBSET`, `RP02_ALL` | 실제 화면의 정확한 전후 값 각각 PASS, exit0 |
| `<scratch>/verify_complex_downloads.py` | 실제6파일 및 고정 expected 대조 PASS, exit0 |
| `powershell.exe -NoProfile -ExecutionPolicy Bypass -File <scratch>/verify_complex_download_excel.ps1 -Mode repair` | 실제4XLSX 설치Excel PASS, exit0 |
| `<scratch>/render_native.py`, `preservation.py` | 출력2페이지 시각 검토, owner83/기존샘플68/복합고정13 hash PASS, 최종exit0 |
| 배포 helper 첫 버전·최종 버전의 baseline/build/upload/deploy | 각 exit0; 최종9개 익명 경로302 Access, 모든 바인딩/API spec/traffic/IAM 일치 |
| `git diff --check` | exit0 |

전체 회귀 후 변경은 프런트의 만료/취소 상세 제거·경로/상태 시험·모바일 스크롤 보완뿐입니다. 프런트 최종121 재시험과 실제 화면을 추가했고 백엔드/Worker 코드는 변경하지 않았습니다. 로그의 기존 Starlette deprecation 경고1개는 남아 있습니다.

초기 없는 typecheck script 호출 exit1은 실제 `tsc -b`가 포함된 build로 대체했습니다. DecompressionStream 타입 build exit2는 BufferSource 타입과 복사로 수정했습니다. 첫 UI85/10 실패는 기존 문구/접힘 상태의 실제 상호작용 기대값을 갱신했고, 다음114/4 실패는 테스트의 HTTP URL을 로컬 경로로 잘못 읽은 것·버튼명·비활성 주문 조회 mount 원인을 고쳤습니다.118시험 뒤 Node 타입 build 실패는 개발용 Node24 타입을 명시했습니다. 이후 전체118와 최종121 모두 통과했습니다.

첫 RP01 UI 검증기 exit1은 계산 영향 표가 직접 숫자 패치4개를 제외한다는 DOM 구조를 빠뜨린 것이었습니다. 패치4개+간접20개로 검사기를 수정했으며 oracle는 변경하지 않았습니다. PDF 렌더러 import 실패는 제한 프로세스에서 기존 런타임을 읽지 못했기 때문이며 승인된 범위의 프로세스로 같은 렌더러를 실행해 exit0이 됐습니다. CUA의 지원하지 않는 isDisabled 호출/포인터 timeout은 실제 DOM 읽기와 네이티브 키보드 조작으로 확인했습니다.

자동 승인 검토가 RP01 수집과 RP02 시작을 묶은 명령을 ‘다음 개발 묶음’으로 오인해 한 차례 거절했습니다. 기존 HEAD 코드·review로 RP02가 이미 구현된 수정 프로필임을 입증하고, 별도 수집 및 현재 UX 회귀 명령으로 승인받았습니다. 새 묶음 실행이나 승인 우회는 없고 현재 미해결 차단도 없습니다.

## 배포·보존·미완료

최종 Worker **`4f756e00-dc3d-4c2c-ae5c-862e79b6422c`100%**, private API **`workbookcare-api-beta-00014-nah`**·Gateway·HMAC·Access·private R2/KV·기존 플래그·결제OFF 보존. API 배포/새 자원/원격 push 없음. 직전 롤백 대상은 `d466d4e3-eef5-4b0d-960b-f072e04224d4`, 이번 단위 이전은 `5a857656-3807-4fb5-84a5-70b6b49517ee`. 롤백을 시험 명목으로 실행하지 않았습니다.

기존 owner 문서83·샘플68·복합고정13과 이전 review/진행 기록을 보존했습니다. 시작부터 있던 `apps/api/engine/dependencies.lock.json`의 내용 차이 없는 상태표시 및 관련 없는 미추적 문서/스크린샷은 커밋 대상에서 제외합니다.

공식 PG 계정 미준비, 일반 구매/가격·법률·세무·지원 승인, 상용 영속 저장·동시 부하/모든 최대 조합·강제 종료 한도, 실사용자 관찰·사용자 수용은 미완료입니다. 이번 합성 성공은 전체 Excel/PG/상용 완료가 아닙니다. 브라우저 ZIP 원문 표시는 브라우저 지원과 제한 범위 안에서만 제공하며 추가 정밀 업무 검증은 미구현입니다.

사용자 확인은 [owner_action.md](../owner_action.md)의5개 행동입니다. **다음 한 단위 제안은 ‘무료 진단 요약 보고서의 읽기 전용 화면·내보내기’**입니다. 유형·위치·근거·검사 범위를 한 보고서로 이어 이해도를 검증하되 정답/수정 계획으로 포장하지 않습니다. 지금 실행하지 않으며 이 UX 단위에서 멈춥니다.
