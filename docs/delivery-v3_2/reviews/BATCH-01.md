# BATCH-01 — APPROVED / implementation

Owner approval 2026-09-16: combine existing numeric-text cleanup and blank-formula restoration in one selected repair plan; grouped expandable details. EFFICIENCY-01 applies.

Outcome: upload once, collect confirmed groups across sheets, calculate all selected changes together, explicitly approve one exact plan and receive one three-file delivery. No new repair rule, limit increase, prices/payment, general sale, public exposure or remote push.

Acceptance (3):
1. Existing RP01/RP02 policies form one bounded combined plan with deterministic targets, original-source validation, overlap/conflict rejection, combined recalculation, one digest/approval/delivery. Existing single-policy flows remain compatible. Selection edits invalidate approval; no partial silent execution.
2. UI keeps a selection basket across sheet/type changes and groups plan details by sheet/type using accessible disclosures. Summary, actual combined impact and required warnings remain visible. Exact full changes stay available and explicit approval remains separate. No required cell-address typing; upload not repeated to adjust the same source before execution.
3. Targeted integration/negative tests, final full regression once, then one actual protected-beta combined flow desktop/mobile/keyboard and actual three downloads compared with an independent frozen combined oracle. Original/non-target OOXML preserved; same relevant structural/M4 detection verifies resolution. Reuse unchanged legacy evidence; do not replay both separate deliveries. No fresh Excel COM claim unless actually run.

Single builder source allowlist: apps/api/app/delivery*.py and corresponding delivery tests; apps/web/src/App.tsx, components/RepairProposalPicker.tsx, ProposalExample.tsx, DeliveryWorkspace.tsx, RepairPlanPreview.tsx, RepairDelivery.tsx and affected related tests/lib repairReview.ts/repairProposals.ts; minimal existing shared types/styles as needed with reported reason. New focused batch helpers/tests allowed in these directories. No dependencies.lock.json edits, no old expected/sample changes. PL owns operating records and separate new oracle/release evidence. No concurrent source writers.

Reuse current calculation/patch/delivery contracts and synthetic complex03. Targeted tests first; builder owns single final scripts/verify.ps1 run after implementation. Reviewer reviews diffs/logs once; only concrete findings get recheck. Same cause two failed fixes: stop/report per EFFICIENCY-01. Standing protected beta deployment authorized; preserve all Access/private API/storage/payment boundaries. No push. Selective local commit after PASS, stop before next unit.
## Independent verification preparation

Frozen combined oracle: DigitalTwin/.tmp/workbookcare-batch01/combined-oracle.json plus SHA256; derived with Decimal arithmetic from existing frozen input rows, no product imports. Legacy RP01/RP02 expected values each725 crosschecked. Combined11 patches,42 indirect impacts,J1302177677,F31/64/1075232/3551/7800. Existing source/oracle unchanged. prepare_oracle.py exit0; deploy_api.py baseline exit0 (read-only existing private-beta settings). Owner explicitly approved synthetic source/output PowerShell Excel COM, macros/external-link updates disabled, read-only/no-save; execution pending implementation. Prepared helper scripts are not product validation.

PL scope clarification: minimal combined-plan reselection compatibility in apps/api/app/payment_plan_actions.py is allowed; no payment/pricing behavior changes. Actual fresh compatibility reference registration in delivery_patch_reference.json is allowed only after measured Excel proof, not before. Existing report template/shared styles may change only for grouped combined summary. Builder tests must use apps/api/.venv/Scripts/python.exe from apps/api with approved escalation, avoiding repeated global-Python/dependency probes.

PL inspected and allowed the minimal payment_service.py policy_base hash call-site adaptation paired with delivery_rights.py; six-line diff only, no payment behavior/price enablement. Scope remains combined target-subset validation.

## STOPPED — cost-control gate / CHANGES_REQUIRED

Initial implementation and one consolidated correction both failed the same-source reuse and nested criteria/target-binding acceptance. EFFICIENCY-01 requires owner decision before more correction/model escalation. No product commit, image upload, API/Worker deployment or remote push. All unfinished changes retained; old fixtures and user dependency lock unchanged.

Actual passing evidence: targeted API input/plan/patch suite exit0 and focused web25 tests exit0 (builder handoff JSON). Initial combined bootstrap matched independent11patch/42indirect/725value oracle. Installed Excel16.0 build20326 opened source+repaired+changes read-only and verified725values, restored formulas and literal report cells, exit0 after correcting PowerShell UTF-8 BOM sheet-name parsing (first helper exit1). That measured proof belongs only to fingerprint4c3bdd26 and is preserved under before-review-fixes; currenta0f66d87 remains STALE. No stale evidence stamped as current PASS.

Remaining: actual restart deletes source job/remounts (new isolated test misses real path); business-criteria hashes are not bound to each candidate, allowing role/anchor criteria swaps between targets; order restore lacks nested check. Also fix optional boolean/combined policy TypeScript signatures and unused Python import before actual type/Ruff verification. Preview crash, both-type summary and Korean text are fixed.

Initial environment attempts used wrong Python/dependencies and sandbox temp permissions; later venv/escalated targeted runs passed. Old pending session65244 recovered exit0; final API1434 exit0. Full regression intentionally not started until valid code/compatibility gate. Prior builder summaries, independent oracle, intermediate measured proof and final CHANGES_REQUIRED evidence retained. [Structured evidence](evidence/BATCH-01.json).

Only proposed continuation: one owner-approved focused correction of remaining blockers and build cleanup, then affected verification; no new feature. Await owner decision.
