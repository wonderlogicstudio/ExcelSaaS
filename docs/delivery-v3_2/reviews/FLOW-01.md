# FLOW-01 — APPROVED / baseline and actual beta verification

2026-09-15 owner: “Excel을 모르는 사람이 기존 두 수정 기능을 끝까지 사용할 수 있는가”를 실제 화면에서 검증 시작하자.

Customer outcome: upload a synthetic workbook, understand a supported numeric-text cleanup or blank-formula restoration, approve exact changes, receive a separate repaired XLSX, change-log XLSX and revalidation HTML, and establish resolved/remaining findings with the original detector.

Reuse existing RP01/RP02, proposal/guided flow, frozen samples/expected, detectors, patch/recalculation/approval/delivery. Preserve original/non-target contents, free evidence/location/CSV/risk, M4 uncertainty, comparison separation, private beta/security/payment boundaries. No new repair rule, AI, price, commerce, resources, push, or unrelated redesign.

PL owns operating/progress/review records and actual browser test. Sole builder owns narrowly justified changes in apps/web/src and apps/api/app plus associated tests/scripts; concrete file allowlist supplied after baseline findings, before edits. Frozen expected and user dirty/untracked files are not editable. Reviewer independently checks code and evidence. No overlapping product writes.

Acceptance: primary RP01/RP02 one-group, full-scope, no-opinion paths measured from empty job through all three downloads; <=4 main stages, <=10 required app activations, zero mandatory cell/formula entry or repeated information. Count all disclosures, business choices, consent, exact approval, execution and downloads. Record excess honestly; never weaken approval to meet budget. Human understanding NOT_RUN until actual user observation.

Use current samples/delivery-v3_2 and WorkbookCare_Complex_Validation_2026-09-13 frozen expected, with normal/unsupported/identifier/nonblank/approval negative cases. Inspect actual protected beta desktop/mobile and keyboard; match on-screen expected and downloaded values, originals/non-target preservation; rerun same initial detector on repaired output. Existing installed Excel checks only when actually run. Static checks are not M4 or Excel proof.

Commands: scripts/verify.ps1 baseline and final if changed, targeted affected tests, existing M4 release gate with existing owner waiver only where required. Record exact commands/exits/failures/retests/artifacts and unrun checks in reviews/FLOW-01.md and evidence/FLOW-01.json, owner_action and delivery-progress delta, preserving old records.

Standing protected-beta deployment authorization applies only to necessary approved fixes after local tests. Selectively local commit verified files, no remote push. End with independent review and 3–5 owner actions; stop before next unit. D08 commercial readiness remains PARTIAL.

## Execution checkpoint

Baseline full verify exit0: web139, Worker16, API231, Ruff PASS, M4 exact36. Actual protected Chrome beta login and landing observed. Filechooser event timed out twice before setting files; this is not established as a product failure. Owner asked to select delivery-holdout-v2.xlsx. UI completion/click count/download/Excel/human understanding NOT_RUN. Builder and reviewer are checking the existing same-detector path. No product change, deployment or push. [Evidence](evidence/FLOW-01.json).

## Bounded builder correction order

Baseline passed before edits. Independent reviewer and PL both confirmed `verify_output` reruns only `scan_workbook`, not original M4 `run_formula_audit`, and does not establish approved candidate resolution. Builder may modify only `apps/api/app/delivery_patch.py`, `delivery_artifacts.py`, `delivery_verification_template.html`, `apps/api/tests/test_delivery_patch.py` and directly related delivery execution tests if needed. Reuse original scanner, preserve free/M4 contracts and expected. Compare completed original-detector outcomes with stable rule/sheet/cell identity, record remaining/resolved/new separately, reject incomplete verification/new candidates/unresolved approved initial target candidates. Do not require unrelated candidates to disappear. Explain formula candidates separately from static findings in HTML. Targeted tests, real build/patch/reverify and regression required. No deployment until local verification and review; actual beta acceptance remains pending file selection. Reviewer temp calculation attempt stopped on Windows chmod access denial before calculation; not counted as engine proof and not bypassed.

## Actual baseline beta and bounded UX correction

Both complex03 flows completed on existing beta. Original screen11=8structure+3formula candidates. RP01_ALL8changes/32calculation impacts, J1302159436; RP02_ALL3changes/16calculation positions, F31/F64/F1075232/3551/7800,J130937923. Actual3downloads each collected with SHA256 under DigitalTwin/.tmp/workbookcare-flow01/browser-downloads. Both actual repaired downloads were uploaded through the same beta UI: RP01 returns0structure+3formula; RP02 returns8structure+0formula, both detectors completed. This is actual baseline repair evidence, not proof of the new local report code.

RP01 required18 activations AFTER completed diagnosis; initial file/scan performed by owner is separate and not precisely measured. RP02 required19 from filechooser with already-established scan consent, including selection of second proposal. Manual cell/formula entry0. Required download activations3; a tool-only attempt to batch download while globally busy produced1file (collector exit1), then sequential remaining downloads produced3 (exit0). Initial locator filechooser failures recovered with supported chooser plus CUA AX button click. Other-file button resets the UI and needs a separate file selection click. Browser content export unsupported; actual screenshots were visually inspected in tool output (RP01 desktop1440/mobile390), not falsely claimed as saved files.

PL authorizes minimum demonstrated UX fixes in apps/web/src/components/DeliveryWorkspace.tsx, RepairPlanPreview.tsx, RepairDelivery.tsx and directly related tests: chain successful proposal preflight to calculation without another user command; directly show full exact changes on approval stage; advance successfully approved plan to result stage while retaining separate execution. Preserve business/retention/rehearsal/exact approval disclosures and consent, revision/digest checks, failures and cancellation. Do not remove safety to meet the initial10click target; report remaining excess. Builder remains sole product editor, reviewer independent. Further frontend areas require a concrete justification. Required final verification includes updated actual beta and downloads.

## Latest checkpoint correction

UI baseline two repairs and six actual downloads completed; same original beta detectors confirm RP01 8→0 static with3M4remaining, RP02 3→0M4 with8staticremaining. Independent reviewer verified6hashes and725/725 frozen cached values in each repaired XLSX. Current Excel app reopen NOT_RUN. Earlier phrase “already-established scan consent” was an assumption: actual code and new-tab UI start free scan directly on file selection, with no consent modal. RP02 measured19 required activations from filechooser. Final184820 regression was interrupted/incomplete (exit unknown), never PASS. Reviewer CHANGES_REQUIRED; builder intermediate source requires correction before rerun. No deployment/commit/push.

## Targeted verification and final-review checkpoint

Root targeted pytest test_delivery_patch.py + test_delivery_execution.py: exit0,30passed; same source hashes verified. Fresh compatibility generator exit0 produced two actual patches and six artifacts in DigitalTwin/.tmp/workbookcare-flow01/compatibility, not published READY. Excel reopen NOT_RUN; explicit user approval requested for existing PowerShell/Excel COM method because current browser-control tool does not expose native Excel. No compatibility reference refresh or gate bypass. Final report edits require regenerating these artifacts. Reviewer found static-only headline and missing readable abstention limitations; also automatic approval continuation encounters stale parent maxStep, not covered by simplified test. Builder assigned bounded corrections. Current provider baseline read-only exit0, old API00014-nah/Worker1fa2992b remain live; no new deployment/commit/push.

## 2026-09-15 FLOW-01 — code review passed; regression blocked on Excel evidence

FLOW-01 remains approved but incomplete. Existing protected-beta RP01/RP02 actual screen flows, six downloads and same-beta rescan passed on the pre-change deployment. Each repaired workbook matches725 frozen cached values; RP01 static8→0/M4remaining3, RP02 M4 3→0/staticremaining8. This is not evidence for the new local report/UI deployment. Scoped source corrections and independent code review PASS; final full regression exit1: web141/build and Worker16 PASS, API228passed/12failed at stale-compatibility gates (8 REFERENCE_NOT_VERIFIED,4 PRODUCT_NOT_READY). Separately API Ruff and M4 frozen36-candidate checks exit0. Full regression is not PASS. New source binds same-detector resolution/completeness, truthful abstention, direct exact-change display, automatic calculation, and one-shot approved navigation with safe return/cancel. Existing explicit approval, separate execution and private beta boundaries remain. Fresh actual synthetic compatibility artifacts were generated but not published READY. Actual Excel reopening NOT_RUN; compatibility remains STALE until new measured evidence is registered. Updated-beta screen/release/runtime verification NOT_RUN. Local HTML opening was blocked by browser URL policy; no workaround or visual PASS claimed. No deployment, local commit or remote push this unit. Initial usability target10 activations NOT_MET (baseline RP02=19); updated count and human comprehension NOT_RUN. Awaiting explicit Excel COM method approval only; no next unit approval inferred.

Final regression: `artifacts/verification/flow01/verify-final-20260915-193109.log` (corresponding `.meta.txt`, exit1;12compatibility-dependent API failures). Prior regression `verify-final-20260915-191906.log` failed1web/140passed; matched-intent assertion retained and changed to wait for the async state before the successful web rerun; the subsequent API stage was blocked by missing refreshed Excel evidence. Earlier184820 execution remains interrupted/exit unknown. Protected152file hashes and all prior27progress keys preserved.

Pending method: run prepared `run_excel_bound.py` wrapper around the existing PowerShell/Excel COM check on only four generated RP01/RP02 XLSX, read-only, macros/events/links disabled, full calculation, no save; bind frozen source/current fingerprint/all six artifact hashes before and after. The current computer-control TOOL documentation (not SKILL.md itself) requires explicit user request for a non-cua_repl UI method; native Excel is unavailable in this connection. After permission: actual Excel proof→validated reference registration→relevant complex integration→protected beta release→actual screen/expected/download retest→selective local commit. Keep the unit active and stop before any next unit.

## Owner-approved actual Excel verification

Owner explicitly approved PowerShell/Excel COM on four prepared synthetic XLSX. `run_excel_bound.py`: exit0; actual Excel16.0/build20326 opened and recalculated repaired outputs plus opened literal-text change reports, closed without saving. All six generated artifact hashes unchanged and bound to frozen source/current fingerprint. Independent review PASS exit0. Sole builder registered measured reference; compatibility_status PASS. Complex final service/engine/artifact integration5cases PASS exit0 (RP01_ALL,RP01_SUBSET,RP02_ALL,comparison,comparison-capacity). Final full regression rerunning; no deployment yet. Local image built exit0, independent review PASS, pushedfalse.

## Final protected-beta verification — 2026-09-15

기존 두 수정 기능의 실제 보호 베타 흐름과 다운로드 검증을 마쳤습니다. 숫자 정리8개/합계2,159,436, 빈 수식3개/5,232·3,551·7,800/합계937,923이 화면과 고정 예상값에 일치했습니다. 실제 다운로드6개, 수정본별725개 예상값 및 원본·비대상 보존 검증 PASS. 같은 베타 재업로드에서 RP01 구조8→0/수식3유지, RP02 수식3→0/구조8유지를 확인했습니다. 지원 검사 후 자동 계산, 정확한 변경의 직접 표시, 별도 승인 후 결과 단계 이동을 적용했고 별도 실행·승인 경계는 유지했습니다.

전체 회귀 exit0(web141/Worker16/API240/Ruff/M4exact36), 복잡도 통합5사례 exit0, 승인받은 실제 Excel4파일 읽기 전용 검증 PASS. 이 Excel4개는 호환성 증거용 출력이며 최종 복잡도 다운로드와 구분합니다. API00017-xid와 Workerc6e1ed05를 기존 보호 베타에 배포했습니다. 원격 Git push 없음.

UX는 부분 충족입니다: 두 경로 모두4단계·필수 앱 조작16회·필수 셀/수식 입력0회. 10회 목표는 미달이며, 긴 전체 변경/영향 목록과 초보자의 실제 이해도는 남은 과제입니다. 제품 엔진·화면의 기술 검증과 고객 사용성 승인, D08 상용화 판단은 별개입니다. FLOW-01의 검증된 수정과 기록을 선별 local commit한 뒤 멈추며 다음 단위는 승인하지 않았습니다.

Evidence: `artifacts/verification/flow01/ui-final.json`, `download-validation.json`, `browser-downloads/*_FINAL.json`, `excel-bound-evidence.json`, `complex-final/execution.json`, final regression log/meta and `release/`, `web-release/`. Commands exit0: `run_excel_bound.py`; `scripts/verify_complex_delivery.py --out artifacts/verification/flow01/complex-final`; `scripts/verify.ps1`; image build/push, `deploy_api.py stage|proof|live|after`; `release_web.py build|baseline|upload|deploy`; `collect_downloads.py begin|collect RP01_ALL_FINAL|RP02_ALL_FINAL`; `verify_downloads.py`. The executed helper copies retain complete provider arguments; sanitized provider command ledgers retain per-call exit codes. Source snapshot binds11 reviewed files; actual renewed reference separately binds the current patch fingerprint. No expected/contracts/samples/dependency lock edits by this unit.

Full earlier failure records remain: interrupted184820 exit unknown,191906 web async-intent assertion failure corrected without weakening assertion,19310912 stale compatibility failures resolved by actual Excel evidence (not mocks/gate bypass). The first web deployment approval was rejected over the historic formula flag; read-only proof showed identical old/current flags and all bindings under existing Access, and normal automatic review then accepted the same command. No permission bypass or anonymous feature exposure.

UI evidence is actual CUA/Chrome interaction, not package examples. Both source scans11=8+3, exact changes and totals observed,3 downloads each, repaired files reuploaded and both detectors completed. RP01/RP02 each16 app activations; detailed ledger in ui-final.json. Extra keyboard/return/responsive QA is outside the primary count. RP01/RP02 return to stage3 remained there; RP02 keyboard arrows selected3/4 with visible focus. Actual desktop1440/mobile390 preview/download screenshots inspected; viewport restored. Screenshot export unavailable, no saved-image claim. Local downloaded HTML visual opening blocked by browser URL policy, no workaround; HTML hashes/provenance/checks verified offline. Fresh final beta UI normal/unsupported/identifier/nonblank/negative-approval cases NOT_RUN; their existing automated regression coverage reused. Human comprehension and official PG NOT_RUN. No live payment/refund, price decision, customer data, new resources, push or next-unit execution.

Owner checks (only synthetic complex03): (1) see11findings in2groups; (2) choose either sheet proposal without typing cells/formulas and understand the representative before/after; (3) inspect all exact changes/impacts and explicitly approve; (4) receive3files and recheck only approved type disappeared; (5) report confusing wording/long scrolling. Next single proposal only: simplify stage2 repeated decisions and stage3 impact presentation while preserving exact approval, then measure against10activations and observe an actual novice. This is NOT_APPROVED and not started.

Independent final record review PASS exit0 after correcting two stale current progress fields to final2flows/16activations each. Reviewer independently confirmed all6download hashes,725expected values per output, original-detector8→0/3→0 with unrelated findings preserved,11source hashes,152protected hashes,27prior progress keys and five original Markdown histories. Reviewer-side PowerShell Unicode audit attempt failed then escaped-Unicode rerun exit0; no product or expected modification. Technical evidence PASS, UX overall PARTIAL.
