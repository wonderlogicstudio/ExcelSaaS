# APPROVED — Guided steps / diagnosis evidence follow-up

Owner feedback 2026-09-13: sequential tabs and intelligible findings before selection or cost. This bounded UX unit follows the completed core-flow IA; it does not start commercial D09 or a new calculation engine.

1. Four tabs: free diagnosis → repair scope/validation → exact change approval → results. Completed prerequisites unlock later tabs; earlier work stays mounted. Pending, failed, expired and changed plans must not keep approval/delivery unlocked. New source resets gates. Payment/right is not approval; notes are not selection.
2. Preserve grouped/full findings, filters, CSV, risk score and M4 value-free API. On opening a finding, read only its original cell and a bounded set of supplied comparison positions from the same browser File. Show actual formula/text/blank and optional stored calculation cache explicitly as unverified. No extra API call, persistence, logs, network, formula execution or inferred repair formula. Unsupported browser/ZIP/XML/shared formula shows unavailable, never blank/safe. Existing scan validation is not replaced.
3. Use bounded ZIP/stream/XML reading with duplicate/path/encryption/CRC/size checks; no new runtime dependency (Node24 type definitions are development-only for the ZIP fixtures). Test malformed/oversized/content injection/stale-file cases. Render text only. Existing fixed synthetic files/expected values remain immutable.
4. One common interpretation/limitations block above findings; preserve unique per-rule guidance in that location. Original cell differences and practical next action stay at each cell. Rename bulk selection to include items in step2; move reviewed/normal/ignored choices to clearly labeled optional personal notes.
5. Integrate the next-step selection count/CTA with the result list. Step2 lists the selected scope without repeating discovery explanations. Rename preflight to explain that it checks support, file preservation and calculation; preserve explicit source consent, business role and anchor. Prices do not appear on free results. Retain legacy quote code/data but do not treat its hypothesis as a supported quote. Actual validated scope and current price-not-set state belong after support validation.
6. Step3 shows exact existing server plans, changed formulas and actually calculated outcomes; no fabricated free expected values. Advanced business/expected-value precision review remains a distinct unimplemented service, not a mandatory paid step or a sellable placeholder.
7. Baseline95; targeted tab gate/evidence/security tests then web/type/build/full existing verify + M4 equivalent. Actual protected beta desktop/mobile, exact formula strings and frozen counts, RP01/RP02 approval and3files. Keep Access/private API/R2/KV/HMAC/paymentOFF. Record failures, corrections, executed commands, version-scoped evidence and NOT_RUN. Commit selected local changes, preserve prior records and stop for owner review.
